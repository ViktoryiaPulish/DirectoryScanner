<Window x:Class="DirectoryScanner.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DirectoryScanner.Wpf.ViewModels"
        xmlns:conv="clr-namespace:DirectoryScanner.Wpf.Converters"
        xmlns:models="clr-namespace:DirectoryScanner.Core.Models;assembly=DirectoryScanner.Core"
        mc:Ignorable="d"
        Title="Directory Scanner" Height="600" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <conv:TypeToIconConverter x:Key="TypeToIconConverter"/>
        <conv:SizeFormatter x:Key="SizeFormatter"/>
        <conv:PercentageFormatter x:Key="PercentageFormatter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Select Folder" Command="{Binding SelectFolderCommand}" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="10,5"/>
        </StackPanel>

        <TreeView Grid.Row="1" ItemsSource="{Binding Items}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type models:DirectoryItem}" ItemsSource="{Binding SortedChildren}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Type, Converter={StaticResource TypeToIconConverter}}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                        <TextBlock Text=" (" Margin="5,0,0,0"/>
                        <TextBlock Text="{Binding Size, Converter={StaticResource SizeFormatter}}"/>
                        <TextBlock Text=", "/>
                        <TextBlock Text="{Binding Percentage, Converter={StaticResource PercentageFormatter}}"/>
                        <TextBlock Text=")"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>